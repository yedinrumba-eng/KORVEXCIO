# S3.1 — Atributos Retail

## Estado

PARCIAL — código escrito, todavía SIN verificar en un bench Frappe.

## Entrega

- `korvexcio/retail/site_config.py` lee `korvexcio_retail` desde el site config.
- `korvexcio/retail/item_attributes.py` sincroniza atributos y valores solo con
  `enabled: true`, y crea templates/variantes mediante APIs de ERPNext.
- `korvexcio/install.py` y `korvexcio/hooks.py` conectan la sincronización al
  evento `after_migrate`.
- `korvexcio/retail/test_item_attributes.py` cubre default apagado,
  idempotencia y creación de variante.

## Verificación

`git diff --check` no reportó salida.

No verificado: `python -m compileall`, `ruff check` y
`bench --site korvexcio.korvexdev.cc run-tests --app korvexcio`. El Windows no
tiene `python`/`ruff` en PATH; `uv` falló por permisos de su instalación
administrada; el cambio no se desplegó al nodo porque no se hizo push ni copia
directa de código.

## Siguiente acción

Desplegar por el flujo autorizado, ejecutar la suite en el nodo y revisar la
salida real de `create_variant` antes de marcar S3.1 completo. Después iniciar
S3.2; no se adelantaron los demás slices.
